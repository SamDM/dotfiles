################################################################################
# Startup
################################################################################

#
# Startup programs
#

    # set background
    exec feh --bg-scale /run/media/sam/storage-parititon/Dropbox/4_Albums/Screensaver/Nature/alps-sky.jpg
    # launch network manager
    exec --no-startup-id nm-applet
    # start dropbox
    exec dropbox

################################################################################
# Keybindings
################################################################################

# Reminder:
# The symbols used in the modes and i3 bar are from 'font awesome'

# Reminder:
# Mod1 = Alt
# Mod4 = OS key

#
# Basics
#

    # Modifier key
    set $mod Mod4

    # Use Mouse+$mod to drag floating windows to their wanted position
    floating_modifier $mod

    # Start a terminal
    bindsym $mod+Return exec konsole

    # Kill focused window
    bindsym $mod+Shift+q kill

    # Start dmenu/rofi (a program launcher)
    # rofi configs are in ~/.confing/rofi/config
    bindsym $mod+space exec rofi -show run \
        -regex \
        -sidebar-mode \
        -lines 5 \
        -opacity 90 \
        -bw 0 \
        -separator-style none \
        -fullscreen \
        -padding 200

#
# Moving around:
#
    # Move your focus around with $mod+[h|j|k|l], like vim
    bindsym $mod+h focus left
    bindsym $mod+j focus down
    bindsym $mod+k focus up
    bindsym $mod+l focus right
    # or use $mod+[up|down|left|right]
    bindsym $mod+Left  focus left
    bindsym $mod+Down  focus down
    bindsym $mod+Up    focus up
    bindsym $mod+Right focus right

    # _move_ the focused window with the same, but add Shift
    bindsym $mod+Shift+h move left
    bindsym $mod+Shift+j move down
    bindsym $mod+Shift+k move up
    bindsym $mod+Shift+l move right
    # ditto, with arrow keys
    bindsym $mod+Shift+Left  move left
    bindsym $mod+Shift+Down  move down
    bindsym $mod+Shift+Up    move up
    bindsym $mod+Shift+Right move right
#
# Workspaces:
#

    set $ws01 "1 "
    set $ws02 "2 "
    set $ws03 "3 "
    set $ws04 "4"
    set $ws05 "5"
    set $ws06 "6"
    set $ws07 "7"
    set $ws08 "8"
    set $ws09 "9"
    set $ws10 "10 "

    # switch to workspace
    bindsym $mod+1 workspace $ws01
    bindsym $mod+2 workspace $ws02
    bindsym $mod+3 workspace $ws03
    bindsym $mod+4 workspace $ws04
    bindsym $mod+5 workspace $ws05
    bindsym $mod+6 workspace $ws06
    bindsym $mod+7 workspace $ws07
    bindsym $mod+8 workspace $ws08
    bindsym $mod+9 workspace $ws09
    bindsym $mod+0 workspace $ws10
    # move focused container to workspace
    bindsym $mod+Shift+1 move container to workspace $ws01
    bindsym $mod+Shift+2 move container to workspace $ws02
    bindsym $mod+Shift+3 move container to workspace $ws03
    bindsym $mod+Shift+4 move container to workspace $ws04
    bindsym $mod+Shift+5 move container to workspace $ws05
    bindsym $mod+Shift+6 move container to workspace $ws06
    bindsym $mod+Shift+7 move container to workspace $ws07
    bindsym $mod+Shift+8 move container to workspace $ws08
    bindsym $mod+Shift+9 move container to workspace $ws09
    bindsym $mod+Shift+0 move container to workspace $ws10

    # move workspace to other monitor
    bindsym $mod+u move workspace to output left
    bindsym $mod+i move workspace to output right

    # programs with fixed workspace
    assign [class="Firefox"] $ws02

#
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    bindsym $mod+b splith
    bindsym $mod+v splitv

    # Switch the current container between different layout styles
    bindsym $mod+s layout stacking
    bindsym $mod+w layout tabbed
    bindsym $mod+e layout toggle split

    # Make the current focus fullscreen
    bindsym $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $mod+Mod1+space focus mode_toggle

    # move focus to the parent container
    bindsym $mod+a focus parent

    # reload the configuration file
    bindsym $mod+Shift+c reload
    # restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
    bindsym $mod+Shift+r restart
    # exit i3 (logs you out of your X session)
    bindsym $mod+Shift+Mod1+e exec "i3-msg exit"

    # resize window (you can also use the mouse for that)
    mode "resize" {

        bindsym h resize shrink width  10 px or 10 ppt
        bindsym j resize grow   height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow   width  10 px or 10 ppt

        bindsym Left  resize shrink width  10 px or 10 ppt
        bindsym Down  resize grow   height 10 px or 10 ppt
        bindsym Up    resize shrink height 10 px or 10 ppt
        bindsym Right resize grow   width  10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
    }

    bindsym $mod+r mode "resize"

#
# Volume
#

    set $mode_volume Volume: j  | k  | m 
    # Reminder (R1): don't quote variables used as mode name, because it will
    # be quoted twice due to the line below.
    mode "$mode_volume" {
        # audio controls
        bindsym k exec bash ~/.config/i3/pactl_helper.bash --up
        bindsym j exec bash ~/.config/i3/pactl_helper.bash --down
        bindsym m exec bash ~/.config/i3/pactl_helper.bash --togmute

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
    }

    bindsym $mod+Control+v mode "$mode_volume"

    # audio control
    bindsym XF86AudioRaiseVolume exec bash ~/.config/i3/pactl_helper.bash --up
    bindsym XF86AudioLowerVolume exec bash ~/.config/i3/pactl_helper.bash --down
    bindsym XF86AudioMute        exec bash ~/.config/i3/pactl_helper.bash --togmute

#
# Media
#

    # Reminder: see (R1) above
    set $mode_media Audio:  h  | s  | w  | i   — Video: l  | d  +  | t 
    mode "$mode_media" {

        # The idea here is that, if you want to change both video and audio,
        # you first choose the video output(s), then the menu will stay and you
        # can immediately choose the audio as well. If you only whish to change
        # the audio output, which will probably happen more often, then you can
        # directly choose the audio and the menu will dissapear immediatly
        # after.
        bindsym h exec bash ~/.config/i3/pa_sound_profiles.bash --headphones-sensible , mode "default"
        bindsym s exec bash ~/.config/i3/pa_sound_profiles.bash --speakers-sensible   , mode "default"
        bindsym w exec bash ~/.config/i3/pa_sound_profiles.bash --woofer-sensible     , mode "default"
        bindsym i exec bash ~/.config/i3/pa_sound_profiles.bash --HDMI-sensible       , mode "default"

        bindsym Return mode "default"
        bindsym Escape mode "default"
    }

    bindsym $mod+Control+m mode "$mode_media"

#
# System options
#

    # Reminder: see (R1) above
    set $mode_system System: l  | e  | s  | r  | p 
    mode "$mode_system" {
        bindsym l exec --no-startup-id sh ~/.config/i3/i3exit.sh lock      , mode "default"
        bindsym e exec --no-startup-id sh ~/.config/i3/i3exit.sh logout    , mode "default"
        bindsym s exec --no-startup-id sh ~/.config/i3/i3exit.sh suspend   , mode "default"
        #bindsym h exec --no-startup-id sh ~/.config/i3/i3exit.sh hibernate , mode "default"
        bindsym r exec --no-startup-id sh ~/.config/i3/i3exit.sh reboot    , mode "default"
        bindsym p exec --no-startup-id sh ~/.config/i3/i3exit.sh shutdown  , mode "default"

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
    }

    bindsym $mod+Control+e mode "$mode_system"

################################################################################
# Appearance
################################################################################

#
# Define colors
#

    set $lime     #9EFD68
    set $snow     #DDECEF
    set $skyblue  #BFD9DA
    set $rain     #87AAAE
    set $sea      #65AA8C
    set $darksea  #6F848F
    set $blush    #BC4676
    set $matte    #F6F6F6
    set $grey1    #B8B9BB
    set $grey3    #2F343F
    set $grey4    #17181A

#
# Windowing options
#
    # Remove title bars
    new_window pixel 1
    # Font for window titles.
    font pango:DejaVu Sans Mono, Awesome 9

    # Window Colours
    # class                 border   backgr.  text     indicator  child_border
    client.focused          $sea     $sea     $grey3   $lime     $sea
    client.focused_inactive $grey3   $grey3   $sea     $lime     $grey3
    client.unfocused        $grey3   $grey3   $skyblue $lime     $grey3
    client.urgent           $blush   $grey3   $blush   $lime     $blush
    client.placeholder      $grey3   $grey3   $skyblue $lime     $grey3

    client.background       $grey3

#
# Gaps and borders
#

    # The 'gaps inner' option also does outer gaps, 'gaps outer' is extra
    # padding on the outside
    gaps inner 4
    gaps outer 0
    # Only show gaps and borders if there is more than one window (container)
    smart_gaps on
    smart_borders on

#
# Bar appearance
#

    bar {
        font pango:DejaVu Sans Mono, Awesome 10
        #separator_symbol " "
        height 24

        #mode hide
        #modifier Mod4

        #status_command i3blocks -c ~/.config/i3/i3blocks.conf
        status_command python ~/.config/i3/i3pystatus_conf.py

        # Bar colors
        colors {
            background $grey3
            statusline $skyblue
            separator  $grey3

            # class            #border  #backgr. #text
            focused_workspace  $grey3   $grey3    $sea
            active_workspace   $grey3   $grey3    $skyblue
            inactive_workspace $grey3   $grey3    $darksea
            urgent_workspace   $blush   $blush    $grey4
            binding_mode       $blush   $blush    $grey4
        }
    }
