set -g default-terminal "xterm-256color"
# This forces Tmux to send Italics (\E[3m) and TrueColor (Tc) 
# to the outer terminal, regardless of what terminfo says.
set -g terminal-overrides ",*xterm*:Tc"

